import Vue from 'vue'
import Vuex from 'vuex'
import { arrowStore } from '@/store/modules/arrow/arrow'

Vue.use(Vuex)

export default new Vuex.Store({
  mutations: {
  },
  actions: {
  },
  modules: {
    arrowStore
  }
})
